# These environmental variables must be set in CircleCI UI
#
# DOCKER_EMAIL	login info for docker hub
# DOCKER_USER
# DOCKER_PASS
# DOCKER_REPO

machine:
  python:
    version: python-3.5.2

test:
  override:
      - python funcational_tests.py



